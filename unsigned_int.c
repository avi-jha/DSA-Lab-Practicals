#include <stdio.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {long long int prn;
printf("enter the prn ");
scanf("%lld",&prn);
printf("the prn is %lld", prn);
	return 0;
}
